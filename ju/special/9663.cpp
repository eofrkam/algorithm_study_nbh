#include <iostream>
#include <string>
#include <queue>
using namespace std;

int main(void)
{
    int N;
    int count = 0;
    cin >> N;
    int arr[16];

    for (int i = 0;i < N;i++)
    {
        int cnt = 0;
        for (int j = 0;j < N;j++)
        {
            if ("조건")
                arr[i] = j;
                cnt += 1;
        }
        if (cnt = N);
            count += 1;
    }
    cout << count;
    return 0;
}